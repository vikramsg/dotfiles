# Shortcut reference (prefix: Ctrl+Space)
# Ctrl+H / Ctrl+J / Ctrl+K / Ctrl+L: focus left / down / up / right
# prefix+q: reload configuration
# prefix+?: show help
# prefix+d: detach
# prefix+[: enter copy mode
# prefix+c: create a tab
# prefix+1..9: switch tabs
# prefix+Shift+1..9: focus agents
# prefix+p / prefix+n: previous / next tab
# prefix+$: rename the current tab
# prefix+" / prefix+%: stacked / side-by-side split
# prefix+x / prefix+z: close / zoom pane
# prefix+;: focus the last pane
# prefix+Shift+C: create a workspace
# prefix+h / prefix+l / prefix+k / prefix+j: resize left / right / down / up
# prefix+Left / prefix+Down / prefix+Up / prefix+Right: focus left / down / up / right
# prefix+Shift+H/J/K/L: swap left / down / up / right
# prefix+g: open LazyGit popup
# prefix+e: toggle nvim sidebar
# prefix+v: pick and watch a Markdown file
# prefix+w: Open workspace picker
# prefix+Shift+T: open terminal popup
# prefix+Shift+D: open Hunk diff review in a new tab
# prefix+Shift+G: open PR review tab
# prefix+f: open nvim file picker
# prefix+r: toggle reviewr pane
# prefix+Shift+O: append OpenCode and Vim project tabs

onboarding = false

[theme]
name = "tokyo-night"

[theme.custom]
accent = "#89b4fa"
sidebar_bg = "#20232b"
active_row_bg = "#24283b"
selection_bg = "#33467c"

[terminal]
default_shell = "/bin/zsh"
new_cwd = "follow"

[keys]
prefix = "ctrl+space"
reload_config = "prefix+q"
help = "prefix+?"
detach = "prefix+d"
copy_mode = "prefix+["
new_tab = "prefix+c"
switch_tab = "prefix+1..9"
focus_agent = "prefix+shift+1..9"
previous_tab = "prefix+p"
next_tab = "prefix+n"
goto = "prefix+s"
edit_scrollback = ""
rename_tab = "prefix+$"
close_pane = "prefix+x"
zoom = "prefix+z"
last_pane = "prefix+semicolon"
split_horizontal = 'prefix+"'
split_vertical = "prefix+%"
new_workspace = "prefix+shift+c"

# Match vim-tmux-navigator's spatial pane movement.
cycle_pane_previous = []
cycle_pane_next = []

# dotfiles.nav-wrap owns Ctrl+h/j/k/l through the plugin actions below.
# Prefix h/j/k/l remain reserved for the tmux-compatible resize commands.
# Prefix arrows move focus between Herdr panes (Herdr-native, no wrap).
focus_pane_left = "prefix+left"
focus_pane_down = "prefix+down"
focus_pane_up = "prefix+up"
focus_pane_right = "prefix+right"

swap_pane_left = "prefix+shift+h"
swap_pane_down = "prefix+shift+j"
swap_pane_up = "prefix+shift+k"
swap_pane_right = "prefix+shift+l"

[[keys.command]]
key = "ctrl+h"
type = "plugin_action"
command = "dotfiles.nav-wrap.left"
description = "navigate left (vim/herdr)"

[[keys.command]]
key = "ctrl+j"
type = "plugin_action"
command = "dotfiles.nav-wrap.down"
description = "navigate down (vim/herdr)"

[[keys.command]]
key = "ctrl+k"
type = "plugin_action"
command = "dotfiles.nav-wrap.up"
description = "navigate up (vim/herdr)"

[[keys.command]]
key = "ctrl+l"
type = "plugin_action"
command = "dotfiles.nav-wrap.right"
description = "navigate right (vim/herdr)"

# chmarax.herdr-nvim: nvim sidebar and agent-output file picker.
# Navigation/sidebar troubleshooting: see README.md.
[[keys.command]]
key = "prefix+e"
type = "plugin_action"
command = "chmarax.herdr-nvim.toggle"
description = "toggle nvim sidebar"

[[keys.command]]
key = "prefix+f"
type = "plugin_action"
command = "chmarax.herdr-nvim.pick-file"
description = "open file from agent output"

# persiyanov.reviewr: code-review pane for agent-written diffs.
[[keys.command]]
key = "prefix+r"
type = "plugin_action"
command = "persiyanov.reviewr.toggle"
description = "toggle reviewr pane"

[[keys.command]]
key = "prefix+h"
type = "shell"
command = '"$HERDR_BIN_PATH" pane resize --direction left --pane "$HERDR_ACTIVE_PANE_ID"'
description = "resize pane left"

[[keys.command]]
key = "prefix+l"
type = "shell"
command = '"$HERDR_BIN_PATH" pane resize --direction right --pane "$HERDR_ACTIVE_PANE_ID"'
description = "resize pane right"

[[keys.command]]
key = "prefix+k"
type = "shell"
command = '"$HERDR_BIN_PATH" pane resize --direction down --pane "$HERDR_ACTIVE_PANE_ID"'
description = "resize pane down"

[[keys.command]]
key = "prefix+j"
type = "shell"
command = '"$HERDR_BIN_PATH" pane resize --direction up --pane "$HERDR_ACTIVE_PANE_ID"'
description = "resize pane up"

# Custom terminal tool launchers.
[[keys.command]]
key = "prefix+g"
type = "popup"
command = "lazygit"
description = "open LazyGit popup"
width = "90%"
height = "90%"

[[keys.command]]
key = "prefix+v"
type = "popup"
command = "leaf --watch"
description = "open Markdown preview"
width = "90%"
height = "90%"

[[keys.command]]
key = "prefix+shift+t"
type = "popup"
command = 'exec "${SHELL:-/bin/zsh}"'
description = "open terminal popup"
width = "90%"
height = "90%"

[[keys.command]]
key = "prefix+shift+d"
type = "shell"
# Restore the originating tab before the Hunk pane exits, rather than letting
# Herdr select the adjacent tab. A closed origin must not prevent Hunk exiting.
command = '''
response=$("$HERDR_BIN_PATH" tab create --workspace "$HERDR_ACTIVE_WORKSPACE_ID" --cwd "$HERDR_ACTIVE_PANE_CWD" --label Hunk --env HERDR_HUNK_RETURN_TAB_ID="$HERDR_ACTIVE_TAB_ID" --env HERDR_BIN_PATH="$HERDR_BIN_PATH" --focus) &&
pane_id=$(printf "%s\n" "$response" | jq -er ".result.root_pane.pane_id") &&
"$HERDR_BIN_PATH" pane run "$pane_id" "exec zsh -ic 'hunk; result=\$?; \"\$HERDR_BIN_PATH\" tab focus \"\$HERDR_HUNK_RETURN_TAB_ID\" >/dev/null 2>&1; exit \"\$result\"'"
'''
description = "open Hunk diff review in a new tab"

[[keys.command]]
key = "prefix+shift+g"
type = "shell"
command = '"$HERDR_BIN_PATH" tab create --workspace "$HERDR_ACTIVE_WORKSPACE_ID" --cwd "$HERDR_ACTIVE_PANE_CWD" --label Review --env HERDR_START_TUICR=1 --focus'
description = "open PR review in a new tab"

[[keys.command]]
key = "prefix+shift+o"
type = "shell"
command = 'project_cwd="$HERDR_ACTIVE_PANE_CWD" && response=$("$HERDR_BIN_PATH" tab create --workspace "$HERDR_ACTIVE_WORKSPACE_ID" --cwd "$project_cwd" --label OpenCode --no-focus) && opencode_tab=$(printf "%s\n" "$response" | jq -r ".result.tab.tab_id") && opencode_pane=$(printf "%s\n" "$response" | jq -r ".result.root_pane.pane_id") && "$HERDR_BIN_PATH" pane run "$opencode_pane" "exec opencode2" && response=$("$HERDR_BIN_PATH" tab create --workspace "$HERDR_ACTIVE_WORKSPACE_ID" --cwd "$project_cwd" --label nvim --no-focus) && vim_pane=$(printf "%s\n" "$response" | jq -r ".result.root_pane.pane_id") && "$HERDR_BIN_PATH" pane run "$vim_pane" "exec nvim ." && "$HERDR_BIN_PATH" tab focus "$opencode_tab"'
description = "append OpenCode and Vim project tabs"

[ui]
tab_bar_position = "top"
sidebar_width = 30
sidebar_min_width = 24
sidebar_max_width = 40
tab_bar_right = [
  { type = "zoom" },
  { type = "command", command = 'branch=$(git branch --show-current 2>/dev/null); test -n "$branch" && printf " %s\n" "$branch"', interval_seconds = 5, timeout_seconds = 2 },
  { type = "command", command = 'host=$(hostname -s); printf "󰒋 %s\n" "${host#dev-vm-}"', interval_seconds = 3600, timeout_seconds = 2 },
]
tab_bar_right_separator = "   "
pane_borders = true
pane_gaps = false
pane_scrollbars = false
mouse_capture = true
copy_on_select = true
confirm_close = true
prompt_new_tab_name = false

[ui.sidebar.spaces]
rows = [
  ["state_icon", { token = "workspace", fg = "#7aa2f7", bold = true }],
  [{ token = "$branch_label", fg = "#9ece6a", dim = true }],
]

[ui.sound]
enabled = false

[experimental]
kitty_graphics = true

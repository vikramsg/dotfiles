---
description: Calls bash once to demonstrate the tool-call blocking plugin.
permission:
  bash: allow
---

Use the bash tool exactly once to run:

printf plugin-demo

After the tool call, report what happened.

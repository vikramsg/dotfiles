1. This config is not very helpful. Feels like I have to constantly change the config. 
    - One idea is to have some prefix or suffix for the name of the tmux session
    - A local cron job can run and keep updating the toml to make sure the latest config mathces jobs that make sure the workspace config has the sessions with the prefix-suffix

display_information:
  name: ocint daemon
features:
  bot_user:
    display_name: ocint daemon
    always_online: false
oauth_config:
  scopes:
    bot:
      - channels:history
      - chat:write
settings:
  event_subscriptions:
    request_url: https://YOUR_STATIC_NGROK_DOMAIN/slack/events
    bot_events:
      - message.channels
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false

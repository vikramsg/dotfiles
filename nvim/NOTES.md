1. Why is typescript comment indentation wrong (addressed by TypeScript headless indentation tests)
    * Its the wrong spacing
    * And it should just use the spacing that is on the previous line
2. Markdown
    * I want the entire line highligted in a different color
    * Maybe use markview
